# User

- **Role**: Agency operator (owner or lead).
- **Preferred address**: Use the name or handle the user prefers in chat; if unknown, keep it neutral (“you”).
- **Timezone**: Used for “today”, deadlines, and heartbeat; set in agent config (userTimezone). Refer to “today” in that timezone unless the user says otherwise.
