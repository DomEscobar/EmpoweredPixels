<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title><%= project %> Kanban</title>
    <style>
        * { box-sizing: border-box; }
        
        :root {
            --bg: #0d1117;
            --surface: #161b22;
            --surface-hover: #1c2128;
            --border: #30363d;
            --text: #c9d1d9;
            --text-muted: #8b949e;
            --accent: #58a6ff;
            --success: #238636;
            --warning: #f0883e;
            --danger: #da3633;
            --priority-high: #ff4444;
            --priority-medium: #f0883e;
            --priority-low: #58a6ff;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background: var(--bg);
            color: var(--text);
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }
        
        /* Header */
        .header {
            background: var(--surface);
            border-bottom: 1px solid var(--border);
            padding: 12px 16px;
            position: sticky;
            top: 0;
            z-index: 100;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .header h1 {
            font-size: 18px;
            margin: 0;
            font-weight: 600;
        }
        
        .header .badge {
            background: var(--accent);
            color: white;
            padding: 4px 10px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 600;
        }
        
        /* Mobile-first: vertical column layout */
        .board {
            padding: 12px;
            display: flex;
            flex-direction: column;
            gap: 16px;
        }
        
        .column {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 12px;
            overflow: hidden;
        }
        
        .column-header {
            padding: 14px 16px;
            background: var(--surface-hover);
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            justify-content: space-between;
            cursor: pointer;
            user-select: none;
            -webkit-tap-highlight-color: transparent;
        }
        
        .column-header:active {
            background: var(--border);
        }
        
        .column-title {
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 600;
            font-size: 14px;
            text-transform: capitalize;
        }
        
        .column-count {
            background: var(--border);
            color: var(--text-muted);
            padding: 2px 8px;
            border-radius: 10px;
            font-size: 12px;
            font-weight: 600;
        }
        
        .column-arrow {
            transition: transform 0.2s;
            color: var(--text-muted);
            font-size: 12px;
        }
        
        .column.collapsed .column-arrow {
            transform: rotate(-90deg);
        }
        
        .column.collapsed .task-list {
            display: none;
        }
        
        .task-list {
            padding: 12px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        
        /* Task Cards */
        .task-card {
            background: var(--bg);
            border: 1px solid var(--border);
            border-radius: 10px;
            padding: 14px;
            cursor: pointer;
            transition: all 0.15s ease;
            position: relative;
            overflow: hidden;
        }
        
        .task-card::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 4px;
            background: var(--priority-medium);
        }
        
        .task-card.priority-high::before { background: var(--priority-high); }
        .task-card.priority-medium::before { background: var(--priority-medium); }
        .task-card.priority-low::before { background: var(--priority-low); }
        
        .task-card:active {
            transform: scale(0.98);
            background: var(--surface-hover);
        }
        
        .task-header {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            gap: 8px;
            margin-bottom: 8px;
        }
        
        .task-id {
            font-size: 11px;
            color: var(--accent);
            font-weight: 600;
            font-family: monospace;
        }
        
        .task-priority {
            font-size: 10px;
            padding: 3px 8px;
            border-radius: 4px;
            font-weight: 600;
            text-transform: uppercase;
        }
        
        .task-priority.high { background: rgba(255,68,68,0.2); color: var(--priority-high); }
        .task-priority.medium { background: rgba(240,136,62,0.2); color: var(--priority-medium); }
        .task-priority.low { background: rgba(88,166,255,0.2); color: var(--priority-low); }
        
        .task-title {
            font-size: 15px;
            font-weight: 600;
            line-height: 1.4;
            margin-bottom: 10px;
            color: var(--text);
        }
        
        .task-desc {
            font-size: 13px;
            color: var(--text-muted);
            line-height: 1.5;
            margin-bottom: 12px;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        .task-footer {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 8px;
        }
        
        .task-labels {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
        }
        
        .label {
            font-size: 10px;
            padding: 4px 8px;
            border-radius: 12px;
            background: var(--surface-hover);
            color: var(--text-muted);
            font-weight: 500;
        }
        
        .label.bug { background: rgba(218,54,51,0.2); color: #ff7b72; }
        .label.feature { background: rgba(35,134,54,0.2); color: #7ee787; }
        .label.enhancement { background: rgba(88,166,255,0.2); color: var(--accent); }
        .label.backend { background: rgba(139,148,158,0.2); color: var(--text); }
        .label.frontend { background: rgba(210,153,34,0.2); color: #ffa657; }
        .label.database { background: rgba(139,80,195,0.2); color: #d2a8ff; }
        .label.shop { background: rgba(47,129,247,0.2); color: #79c0ff; }
        .label.routing { background: rgba(139,148,158,0.2); color: var(--text); }
        .label.payments { background: rgba(35,134,54,0.2); color: #7ee787; }
        
        .assignee {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 12px;
            color: var(--text-muted);
        }
        
        .assignee-avatar {
            width: 22px;
            height: 22px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--accent), var(--success));
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
            font-weight: 600;
            color: white;
        }
        
        .assignee-avatar.unassigned {
            background: var(--border);
        }
        
        /* Empty state */
        .empty-state {
            text-align: center;
            padding: 30px 20px;
            color: var(--text-muted);
            font-size: 13px;
        }
        
        /* Tablet/Desktop: horizontal scroll */
        @media (min-width: 768px) {
            .board {
                flex-direction: row;
                overflow-x: auto;
                align-items: flex-start;
                gap: 16px;
                padding: 20px;
            }
            
            .column {
                min-width: 320px;
                max-width: 320px;
            }
            
            .column-header {
                cursor: default;
            }
            
            .column-arrow {
                display: none;
            }
            
            .column.collapsed .task-list {
                display: flex;
            }
        }
        
        /* Dragging visual feedback */
        .task-card.dragging {
            opacity: 0.5;
        }
        
        /* Animations */
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .task-card {
            animation: slideIn 0.3s ease;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>📋 <%= project %></h1>
        <span class="badge">Sprint <%= meta.currentSprint %></span>
    </div>
    
    <div class="board">
        <% columns.forEach(col => { 
            const tasks = columnTasks[col];
        %>
            <div class="column" data-column="<%= col %>">
                <div class="column-header" onclick="toggleColumn(this)">
                    <div class="column-title">
                        <%= getColumnIcon(col) %>
                        <%= col.replace(/_/g, ' ') %>
                        <span class="column-count"><%= tasks.length %></span>
                    </div>
                    <span class="column-arrow">▼</span>
                </div>
                <div class="task-list">
                    <% if (tasks.length === 0) { %>
                        <div class="empty-state">No tasks</div>
                    <% } else { %>
                        <% tasks.forEach(task => { %>
                            <div class="task-card priority-<%= task.priority %>" data-task-id="<%= task.id %>">
                                <div class="task-header">
                                    <span class="task-id"><%= task.id %></span>
                                    <span class="task-priority <%= task.priority %>"><%= task.priority %></span>
                                </div>
                                <div class="task-title"><%= task.title %></div>
                                <% if (task.description) { %>
                                    <div class="task-desc"><%= task.description %></div>
                                <% } %>
                                <div class="task-footer">
                                    <div class="task-labels">
                                        <% task.labels.forEach(label => { %>
                                            <span class="label <%= label %>"><%= label %></span>
                                        <% }) %>
                                    </div>
                                    <% 
                                        const assigneeName = task.assignee || 'unassigned';
                                        const initials = assigneeName === 'unassigned' ? '?' : assigneeName.substring(0, 2).toUpperCase();
                                    %>
                                    <div class="assignee">
                                        <div class="assignee-avatar <%= assigneeName === 'unassigned' ? 'unassigned' : '' %>"><%= initials %></div>
                                        <% if (assigneeName !== 'unassigned') { %>
                                            <span><%= assigneeName %></span>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } %>
                </div>
            </div>
        <% }) %>
    </div>

    <script>
        // Toggle column collapse on mobile
        function toggleColumn(header) {
            if (window.innerWidth < 768) {
                header.parentElement.classList.toggle('collapsed');
            }
        }
        
        // Auto-collapse empty columns on mobile
        if (window.innerWidth < 768) {
            document.querySelectorAll('.column').forEach(col => {
                if (col.querySelector('.column-count').textContent === '0') {
                    col.classList.add('collapsed');
                }
            });
        }
    </script>
</body>
</html>
<%
function getColumnIcon(col) {
    const icons = {
        'backlog': '📥',
        'todo': '📋',
        'in_progress': '🏃',
        'review': '👀',
        'done': '✅'
    };
    return icons[col] || '📄';
}
%>
