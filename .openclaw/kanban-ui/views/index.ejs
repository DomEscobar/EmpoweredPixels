<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title><%= project %> Kanban</title>
    <link rel="stylesheet" href="/theme.css">
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        * { box-sizing: border-box; }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }
        
        /* Header */
        .header {
            background: rgba(18, 20, 23, 0.8);
            backdrop-filter: blur(10px);
            border-bottom: 2px solid var(--clr-worn-gold-muted);
            padding: 16px 24px;
            position: sticky;
            top: 0;
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 4px 20px rgba(0,0,0,0.5);
        }
        
        .header h1 {
            font-family: var(--font-heading);
            font-size: 24px;
            margin: 0;
            font-weight: 700;
            color: var(--clr-worn-gold);
            text-transform: uppercase;
            letter-spacing: 2px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
        }
        
        .header .badge {
            background: var(--accent);
            color: white;
            padding: 4px 10px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 600;
        }
        
        /* Mobile-first: vertical column layout */
        .board {
            padding: 12px;
            display: flex;
            flex-direction: column;
            gap: 16px;
        }
        
        .column {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 12px;
            overflow: hidden;
        }
        
        .column-header {
            padding: 14px 16px;
            background: var(--surface-hover);
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            justify-content: space-between;
            cursor: pointer;
            user-select: none;
            -webkit-tap-highlight-color: transparent;
        }
        
        .column-header:active {
            background: var(--border);
        }
        
        .column-title {
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 600;
            font-size: 14px;
            text-transform: capitalize;
        }
        
        .column-count {
            background: var(--border);
            color: var(--text-muted);
            padding: 2px 8px;
            border-radius: 10px;
            font-size: 12px;
            font-weight: 600;
        }
        
        .column-arrow {
            transition: transform 0.2s;
            color: var(--text-muted);
            font-size: 12px;
        }
        
        .column.collapsed .column-arrow {
            transform: rotate(-90deg);
        }
        
        .column.collapsed .task-list {
            display: none;
        }
        
        .task-list {
            padding: 12px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        
        .task-card {
            background: rgba(26, 28, 30, 0.4);
            backdrop-filter: blur(4px);
            -webkit-backdrop-filter: blur(4px);
            border: 1px solid rgba(197, 160, 89, 0.2);
            border-radius: 4px;
            padding: 14px;
            cursor: pointer;
            transition: all 0.2s ease;
            position: relative;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.4), inset 0 0 10px rgba(255,255,255,0.02);
        }
        
        .task-card:hover {
            border-color: var(--clr-worn-gold);
            background: rgba(26, 28, 30, 0.6);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.6), inset 0 0 15px rgba(197, 160, 89, 0.1);
        }
        
        .task-header {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            gap: 8px;
            margin-bottom: 8px;
        }
        
        .task-id {
            font-size: 11px;
            color: var(--accent);
            font-weight: 600;
            font-family: monospace;
        }
        
        .task-priority {
            font-size: 10px;
            padding: 3px 8px;
            border-radius: 4px;
            font-weight: 600;
            text-transform: uppercase;
        }
        
        .task-priority.high { background: rgba(255,68,68,0.2); color: var(--priority-high); }
        .task-priority.medium { background: rgba(240,136,62,0.2); color: var(--priority-medium); }
        .task-priority.low { background: rgba(88,166,255,0.2); color: var(--priority-low); }
        
        .task-title {
            font-size: 15px;
            font-weight: 600;
            line-height: 1.4;
            margin-bottom: 10px;
            color: var(--text);
        }
        
        .task-desc {
            font-size: 13px;
            color: var(--text-muted);
            line-height: 1.5;
            margin-bottom: 12px;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        .task-footer {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 8px;
        }
        
        .task-labels {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
        }
        
        .label {
            font-size: 10px;
            padding: 4px 8px;
            border-radius: 12px;
            background: var(--surface-hover);
            color: var(--text-muted);
            font-weight: 500;
        }
        
        .label.bug { background: rgba(218,54,51,0.2); color: #ff7b72; }
        .label.feature { background: rgba(35,134,54,0.2); color: #7ee787; }
        .label.enhancement { background: rgba(88,166,255,0.2); color: var(--accent); }
        .label.backend { background: rgba(139,148,158,0.2); color: var(--text); }
        .label.frontend { background: rgba(210,153,34,0.2); color: #ffa657; }
        .label.database { background: rgba(139,80,195,0.2); color: #d2a8ff; }
        .label.shop { background: rgba(47,129,247,0.2); color: #79c0ff; }
        .label.routing { background: rgba(139,148,158,0.2); color: var(--text); }
        .label.payments { background: rgba(35,134,54,0.2); color: #7ee787; }
        
        .assignee {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 12px;
            color: var(--text-muted);
        }
        
        .assignee-avatar {
            width: 22px;
            height: 22px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--accent), var(--success));
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
            font-weight: 600;
            color: white;
        }
        
        .assignee-avatar.unassigned {
            background: var(--border);
        }
        
        /* Empty state */
        .empty-state {
            text-align: center;
            padding: 30px 20px;
            color: var(--text-muted);
            font-size: 13px;
        }
        
        /* Tablet/Desktop: horizontal scroll */
        @media (min-width: 768px) {
            .board {
                flex-direction: row;
                overflow-x: auto;
                align-items: flex-start;
                gap: 16px;
                padding: 20px;
            }
            
            .column {
                min-width: 320px;
                max-width: 320px;
            }
            
            .column-header {
                cursor: default;
            }
            
            .column-arrow {
                display: none;
            }
            
            .column.collapsed .task-list {
                display: flex;
            }
        }
        
        /* Dragging visual feedback */
        .task-card.dragging {
            opacity: 0.5;
        }
        
        /* Animations */
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .task-card {
            animation: slideIn 0.3s ease;
        }
    </style>
</head>
<body class="texture-stone">
    <div class="vignette-overlay"></div>
    <div class="header">
        <h1><%= project %></h1>
        <div style="display: flex; gap: 12px; align-items: center;">
            <a href="/roster" class="btn-forged" style="text-decoration: none; font-size: 12px; height: 32px;">Foundry Roster</a>
            <span class="badge" style="background: var(--clr-blood-red); border: 1px solid var(--clr-worn-gold-muted); padding: 4px 12px; border-radius: 4px; font-family: var(--font-heading);">Sprint <%= meta.currentSprint %></span>
        </div>
    </div>
    
    <div class="board">
        <% columns.forEach(col => { 
            const tasks = columnTasks[col];
        %>
            <div class="column" data-column="<%= col %>">
                <div class="column-header" onclick="toggleColumn(this)">
                    <div class="column-title">
                        <%= getColumnIcon(col) %>
                        <%= col.replace(/_/g, ' ') %>
                        <span class="column-count"><%= tasks.length %></span>
                    </div>
                    <span class="column-arrow">▼</span>
                </div>
                <div class="task-list">
                    <% if (tasks.length === 0) { %>
                        <div class="empty-state">No tasks</div>
                    <% } else { %>
                        <% tasks.forEach(task => { %>
                            <div class="task-card priority-<%= task.priority %> rarity-<%= (task.labels.includes('feature') || task.labels.includes('epic')) ? 'epic' : (task.labels.includes('enhancement') || task.labels.includes('rare')) ? 'rare' : (task.labels.includes('bug') || task.labels.includes('uncommon')) ? 'uncommon' : 'common' %>" data-task-id="<%= task.id %>">
                                <div class="task-header">
                                    <span class="task-id"><%= task.id %></span>
                                    <span class="task-priority <%= task.priority %>"><%= task.priority %></span>
                                </div>
                                <div class="task-title"><%= task.title %></div>
                                <% if (task.description) { %>
                                    <div class="task-desc"><%= task.description %></div>
                                <% } %>
                                <div class="task-footer">
                                    <div class="task-labels">
                                        <% task.labels.forEach(label => { %>
                                            <span class="label <%= label %>"><%= label %></span>
                                        <% }) %>
                                    </div>
                                    <% 
                                        const assigneeName = task.assignee || 'unassigned';
                                        const initials = assigneeName === 'unassigned' ? '?' : assigneeName.substring(0, 2).toUpperCase();
                                    %>
                                    <div class="assignee">
                                        <div class="assignee-avatar <%= assigneeName === 'unassigned' ? 'unassigned' : '' %>"><%= initials %></div>
                                        <% if (assigneeName !== 'unassigned') { %>
                                            <span><%= assigneeName %></span>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } %>
                </div>
            </div>
        <% }) %>
    </div>

    <script>
        // Toggle column collapse on mobile
        function toggleColumn(header) {
            if (window.innerWidth < 768) {
                header.parentElement.classList.toggle('collapsed');
            }
        }
        
        // Auto-collapse empty columns on mobile
        if (window.innerWidth < 768) {
            document.querySelectorAll('.column').forEach(col => {
                if (col.querySelector('.column-count').textContent === '0') {
                    col.classList.add('collapsed');
                }
            });
        }
    </script>
</body>
</html>
<%
function getColumnIcon(col) {
    const icons = {
        'backlog': '📥',
        'todo': '📋',
        'in_progress': '🏃',
        'review': '👀',
        'done': '✅'
    };
    return icons[col] || '📄';
}
%>
