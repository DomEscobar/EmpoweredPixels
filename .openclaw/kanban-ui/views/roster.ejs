<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roster - EmpoweredPixels</title>
    <link rel="stylesheet" href="/theme.css">
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            color: var(--clr-text);
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 10;
        }

        .back-link {
            display: inline-flex;
            align-items: center;
            color: var(--clr-worn-gold);
            text-decoration: none;
            font-family: var(--font-heading);
            margin-bottom: 20px;
            gap: 8px;
        }

        /* Hero Layout */
        .roster-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 40px;
        }

        @media (min-width: 900px) {
            .roster-grid {
                grid-template-columns: 450px 1fr;
                height: calc(100vh - 120px);
                align-items: center;
            }
        }

        /* Hero Section */
        .hero-display {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            animation: fadeIn 0.8s ease-out;
        }

        .hero-pixel-art {
            width: 300px;
            height: 300px;
            image-rendering: pixelated;
            filter: drop-shadow(0 0 20px rgba(197, 160, 89, 0.3));
            margin-bottom: 20px;
            transition: transform 0.3s ease;
        }

        .hero-pixel-art:hover {
            transform: scale(1.05);
        }

        .hero-name {
            font-family: var(--font-heading);
            font-size: 3rem;
            color: var(--clr-worn-gold);
            margin: 0;
            text-transform: uppercase;
            letter-spacing: 4px;
            text-shadow: 3px 3px 0px rgba(0,0,0,0.8);
        }

        .hero-class {
            font-family: var(--font-heading);
            font-size: 1.2rem;
            color: var(--clr-text-muted);
            margin-bottom: 20px;
            letter-spacing: 2px;
        }

        /* Fighter List */
        .fighter-scroll {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
            gap: 16px;
            padding: 10px;
            overflow-y: auto;
            max-height: 100%;
        }

        .fighter-card {
            cursor: pointer;
            text-align: center;
            padding: 15px;
            border-radius: 4px;
        }

        .fighter-card img {
            width: 80px;
            height: 80px;
            image-rendering: pixelated;
            margin-bottom: 10px;
        }

        .fighter-card h3 {
            margin: 0;
            font-family: var(--font-heading);
            font-size: 14px;
            color: var(--clr-worn-gold);
        }

        .fighter-card:hover {
            transform: translateY(-5px);
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Utility */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            width: 100%;
            margin-top: 20px;
        }

        .stat-box {
            background: rgba(0,0,0,0.4);
            border: 1px solid var(--clr-iron);
            padding: 8px;
            border-radius: 4px;
        }

        .stat-label { font-size: 10px; color: var(--clr-text-muted); text-transform: uppercase; }
        .stat-value { font-family: var(--font-heading); font-weight: bold; color: var(--clr-worn-gold); }

    </style>
</head>
<body class="texture-stone">
    <div class="vignette-overlay"></div>
    
    <div class="container">
        <a href="/" class="back-link">← Return to Board</a>

        <div class="roster-grid">
            <!-- Hero Selection -->
            <div class="hero-display">
                <div class="rpg-card-glass rarity-legendary rpg-border-gold" style="padding: 40px; border-radius: 12px; width: 100%;">
                    <img src="https://vibemedia.space/char_knight_hero.png?prompt=legendary%20hero%20knight%20character%20sprite%20high%20detail%20gold%20armor&style=pixel_game_asset&key=NOGON" alt="Hero" class="hero-pixel-art">
                    <h1 class="hero-name">Alaric</h1>
                    <div class="hero-class">Sun-Forged Paladin</div>
                    
                    <div class="stats-grid">
                        <div class="stat-box">
                            <div class="stat-label">Power</div>
                            <div class="stat-value">88</div>
                        </div>
                        <div class="stat-box">
                            <div class="stat-label">Defense</div>
                            <div class="stat-value">94</div>
                        </div>
                        <div class="stat-box">
                            <div class="stat-label">Magic</div>
                            <div class="stat-value">42</div>
                        </div>
                    </div>

                    <div style="margin-top: 30px;">
                        <button class="btn-forged">Assign to Task</button>
                    </div>
                </div>
            </div>

            <!-- Roster List -->
            <div class="fighter-scroll">
                <div class="fighter-card rpg-card-glass rarity-epic">
                    <img src="https://vibemedia.space/char_mage_01.png?prompt=mage%20character%20sprite%20blue%20robes&style=pixel_game_asset&key=NOGON" alt="Mage">
                    <h3>Elara</h3>
                </div>
                <div class="fighter-card rpg-card-glass rarity-rare">
                    <img src="https://vibemedia.space/char_rogue_01.png?prompt=rogue%20character%20sprite%20dark%20hood&style=pixel_game_asset&key=NOGON" alt="Rogue">
                    <h3>Kael</h3>
                </div>
                <div class="fighter-card rpg-card-glass rarity-uncommon">
                    <img src="https://vibemedia.space/char_warrior_01.png?prompt=warrior%20character%20sprite%20iron%20armor&style=pixel_game_asset&key=NOGON" alt="Warrior">
                    <h3>Thorg</h3>
                </div>
                <div class="fighter-card rpg-card-glass rarity-rare">
                    <img src="https://vibemedia.space/char_archer_01.png?prompt=archer%20character%20sprite%20green%20cloak&style=pixel_game_asset&key=NOGON" alt="Archer">
                    <h3>Lyra</h3>
                </div>
                <!-- Mocking more for scroll demo -->
                <% for(let i=0; i<8; i++) { %>
                    <div class="fighter-card rpg-card-glass rarity-common">
                        <img src="https://vibemedia.space/char_recruit_<%= i %>.png?prompt=simple%20recruit%20character%20sprite&style=pixel_game_asset&key=NOGON" alt="Recruit">
                        <h3>Recruit #<%= i+1 %></h3>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
